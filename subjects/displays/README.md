## displayp

### Instructions

Write a program that displays an `P` character on the standard followed by newlines (`'\n'`)

### Usage

```console
$ go run . | cat -e
P$
$
$ go run . "a" "b" | cat -e
P$
$
$ go run . "a" "b" "c" | cat -e
P$
$
```